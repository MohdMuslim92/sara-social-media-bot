# Core dependencies
python-dotenv==1.0.0       # For loading environment variables
pyyaml==6.0.1              # For reading/writing YAML files
tweepy==4.14.0             # For Twitter API interactions
facebook-sdk==3.1.0        # For Facebook API interactions
pillow==10.3.0             # For image processing (resizing, etc.)
python-dateutil==2.8.2     # For date/time handling (if needed)
requests==2.31.0           # For making HTTP requests (used in FacebookHandler)